import Control from '../../controls/control';

export default class miniGamesSection extends Control {
  constructor(parentNode: HTMLElement) {
    super(parentNode, 'section', 'mini-games-section', 'test mini-games');
  }

}
