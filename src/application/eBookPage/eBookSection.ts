import Control from '../../controls/control';

export default class EBookSection extends Control {
  constructor(parentNode: HTMLElement) {
    super(parentNode, 'section', 'eBook-section', 'test e-book');
  }
}
